# MOMO local environment example
# Copy to `.env` for local-only overrides (never commit `.env`).
# MVP uses mock data — no backend keys required yet.

# Optional local flags for future flavors
# MOMO_ENV=local
