# MOMO environment example
#
# MVP uses mock data only — no backend, auth, or API keys required.
# Copy this file to `.env` only if you add integrations later.
# Never commit real secrets.

# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# API_BASE_URL=
